{
  "name": "poly-trader",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/dashboard",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/dashboard",
    "start": "npm run start --workspace=apps/dashboard"
  }
}
